# This file is used to create a GitHub release and push artifact reference to ds-coordination storage.
# It is designed to be triggered by a push event that creates a new tag matching the pattern 'v*.*.*'.
# The template includes steps for checking out the code, configuring AWS credentials,
# pushing release information to DynamoDB, and creating a GitHub release.
# DS-Coordination repository own the templates for building and releasing Go, and other, applications.

name: Push Release to DynamoDB and Create GitHub Release

permissions:
  contents: write

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  push-release:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: eu-north-1
      DOMAIN: ds
      SERVICE_GROUP: packages
      SERVICE_NAME: go-tools-client
      ACCOUNT_ID: 999125116186

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Extract Metadata
        id: meta
        run: |
          echo "VERSION=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          echo "COMMIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "TIMESTAMP=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      - name: Push Release Fragment to DynamoDB
        run: |
          aws dynamodb put-item \
            --table-name ds-coordination-release-fragments-prod \
            --item '{
              "PK": {"S": "${{ steps.meta.outputs.VERSION }}"},
              "SK": {"S": "${{ env.DOMAIN }}#${{ env.SERVICE_GROUP }}#${{ env.SERVICE_NAME }}"},
              "GSI1PK": {"S": "service#${{ env.SERVICE_NAME }}"},
              "GSI1SK": {"S": "${{ steps.meta.outputs.VERSION }}"},
              "GSI2PK": {"S": "latest#${{ env.DOMAIN }}#${{ env.SERVICE_GROUP }}"},
              "GSI2SK": {"S": "${{ env.SERVICE_NAME }}"},
              "domain": {"S": "${{ env.DOMAIN }}"},
              "serviceGroup": {"S": "${{ env.SERVICE_GROUP }}"},
              "serviceName": {"S": "${{ env.SERVICE_NAME }}"},
              "version": {"S": "${{ steps.meta.outputs.VERSION }}"},
              "commit": {"S": "${{ steps.meta.outputs.COMMIT_SHA }}"},
              "artifact": {"S": "${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.DOMAIN }}-${{ env.SERVICE_GROUP }}-${{ env.SERVICE_NAME }}-prod:${{ steps.meta.outputs.VERSION }}"},
              "timestamp": {"S": "${{ steps.meta.outputs.TIMESTAMP }}"}
            }' \
            --region $AWS_REGION

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.VERSION }}
          name: Release ${{ steps.meta.outputs.VERSION }}
          generate_release_notes: true
          draft: false
          prerelease: false